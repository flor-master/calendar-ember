import { monthFullName } from 'demo/helpers/month-full-name';
import { module, test } from 'qunit';

module('Unit | Helper | month full name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = monthFullName([42]);
  assert.ok(result);
});
